let score = 0
passingGrade = 60

 if (score >= passingGrade) {
    grade = `The passing grade is ${passingGrade} and you scored ${score}. You did TREMENDOUSLY well, that's a PASS! `
} else {
    grade = `The passing grade is ${passingGrade} and you scored ${score}. Are you proud of yourself? FAILED!!!! `
    
 }

 console.log(grade);
 